const express = require('express');
const router = express.Router();

// Placeholder — implemented in Feature 2
router.post('/', (req, res) => {
  res.json({ message: 'Upload route not yet implemented' });
});

module.exports = router;
